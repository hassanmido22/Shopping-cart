<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hassan.shopping_project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.login"
            android:label="Login"
            android:theme="@style/shopping_app">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:name=".activities.registeration"
            android:label="Register"
            android:theme="@style/shopping_app" />
        <activity
            android:name=".activities.FoodActivity"
            android:label="Food">


        </activity>
        <activity
            android:name=".activities.SportsActivity"
            android:label="Sports" />
        <activity
            android:name=".activities.CartActivity"
            android:label="Cart" />
        <activity
            android:name=".activities.shopping_itemss"
            android:label="Shopping items"></activity>
        <activity
            android:name=".activities.history"
            android:label="History" />
        <activity
            android:name=".activities.profile"
            android:label="Profile" />
        <activity android:name=".activities.update_profile"></activity>
    </application>

</manifest>